/**
 * Narrow icon surface shared by custom glyphs and lucide wrappers.
 * Lucide exposes richer props (`size`, `strokeWidth` …) at call sites when needed.
 */
export type IconProps = {
  className?: string
}
