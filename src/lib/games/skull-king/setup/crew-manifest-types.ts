export const MIN_CREW_PLAYERS = 2

/** Skull King table size cap for host scoring UI. */
export const MAX_CREW_PLAYERS = 8

/** Max characters per crew name (setup input and score tables). */
export const MAX_CREW_NAME_LENGTH = 8
