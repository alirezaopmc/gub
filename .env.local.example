# Copy to `.env.local` and adjust. Committed as a template only (no secrets).

# Comma-separated origins allowed for dev cross-origin requests (e.g. testing from a phone on LAN).
# Example:
# NEXT_PUBLIC_DEV_ORIGINS=http://192.168.1.13:3000,http://localhost:3000
NEXT_PUBLIC_DEV_ORIGINS=
